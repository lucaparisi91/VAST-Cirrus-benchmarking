[project]
name = "nemo-env"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lparisi", email = "l.parisi@epcc.ed.ac.uk" }
]
requires-python = ">=3.9"
dependencies = [
    "darshan>=3.5.0",
    "jupyterlab>=4.5.10",
    "pandas>=2.3.3",
]

[project.scripts]
nemo-env = "nemo_env:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
